import styled, { css } from 'styled-components'

export const GalleryBox = styled.div`
  width: ${props => props.width};
  margin: auto;
  padding: 20px;
  h2 {
    text-align: center;
    font-size: 40px;
    letter-spacing: -1px;
    font-weight: 600;
    margin-bottom: 40px;
  }
`
